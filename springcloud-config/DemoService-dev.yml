server:
  port: 18080
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  stream:
    bindings:
      demoMsg:
        group: demo
        content-type: application/json